version: '3.8'
services:
  # The Smart Attendance Spring Boot Application
  smart-attendance-app:
    image: eclipse-temurin:17-jre-alpine
    working_dir: /app
    command: ["java", "-jar", "target/smartAttendence-0.0.1-SNAPSHOT.jar"]
    ports:
      - "8444:8443"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - DB_HOST=postgres-postgis
      - DB_PORT=5432
      - DB_NAME=smart_attendance
      - DB_USERNAME=postgres
      - DB_PASSWORD=${DB_PASSWORD:-postgres}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    volumes:
      - ./target:/app/target
      - ./src:/app/src
    depends_on:
      - postgres-postgis
      - redis
      - kafka
    networks:
      - smart-attendance-network
    restart: unless-stopped

  # The Primary Database with Spatial Muscle (Shifted to Port 5433 to avoid conflicts)
  postgres-postgis:
    image: postgis/postgis:15-3.3
    environment:
      POSTGRES_USER: ${DB_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-smart_attendance}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - smart-attendance-network

  # The High-Speed Memory Buffer (Redis with persistence and optimization)
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - smart-attendance-network
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru

  # Zookeeper for Kafka coordination
  zookeeper:
    image: confluentinc/cp-zookeeper:7.4.0
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    networks:
      - smart-attendance-network

  # The Event Broker (Enhanced Kafka with Zookeeper)
  kafka:
    image: confluentinc/cp-kafka:7.4.0
    depends_on:
      - zookeeper
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: true
    ports:
      - "9092:9092"
    volumes:
      - kafka_data:/var/lib/kafka/data
    networks:
      - smart-attendance-network

  # Kafka UI for management and monitoring
  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    depends_on:
      - kafka
    environment:
      KAFKA_CLUSTERS_0_NAME: smart-attendance-cluster
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:29092
      KAFKA_CLUSTERS_0_ZOOKEEPER: zookeeper:2181
    ports:
      - "8080:8080"
    networks:
      - smart-attendance-network

volumes:
  postgres_data:
  redis_data:
  kafka_data:

networks:
  smart-attendance-network:
    driver: bridge
